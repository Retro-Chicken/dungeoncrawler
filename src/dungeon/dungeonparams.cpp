#include "dungeonparams.h"

namespace params {
	const int MAX_ROOMS = 4;
	const int MIN_ROOMS = 2;
	const int MIN_ROOM_WIDTH = 10, MAX_ROOM_WIDTH = 20;
	const int MIN_ROOM_HEIGHT = 7, MAX_ROOM_HEIGHT = 20;
	const int ROOM_BUFFER = 2;
	const int CORRIDOR_SIZE = 1;
	
	sf::Texture tileTexture;
};
