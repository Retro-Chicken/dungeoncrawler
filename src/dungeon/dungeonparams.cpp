#include "dungeonparams.h"

namespace params {
	const int MAX_ROOMS = 6;
	const int MIN_ROOMS = 3;
	const int MIN_ROOM_WIDTH = 5, MAX_ROOM_WIDTH = 10;
	const int MIN_ROOM_HEIGHT = 5, MAX_ROOM_HEIGHT = 10;
	const int ROOM_BUFFER = 2;
	const int CORRIDOR_SIZE = 1;

	sf::Texture tileTexture;
};
